#!/usr/bin/env python

# coding this way allows the env program to locate Python's interpreter according the 
# system search path settings, so you don't have to hardcode the path

import sys

print(ls)